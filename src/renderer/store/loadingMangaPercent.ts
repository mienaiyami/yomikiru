// import { createSlice, PayloadAction } from "@reduxjs/toolkit";

// const loadingMangaPercent = createSlice({
//     name: "loadingMangaPercent",
//     initialState: 100,
//     reducers: {
//         setLoadingMangaPercent: (state, action: PayloadAction<number>) => {
//             return action.payload > 100 ? 100 : action.payload;
//         },
//     },
// });

// export const { setLoadingMangaPercent } = loadingMangaPercent.actions;

// export default loadingMangaPercent.reducer;
